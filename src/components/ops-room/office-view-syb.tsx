"use client";

// Team Syb V2 floor is now rendered by PixelOffice with ceo={{ id: "syb", naam: "Syb", avatar: "#a855f7" }}
// This component is kept for backward compatibility but the actual rendering
// happens in page.tsx via the floor toggle (V1/V2/Beide).
// See: src/app/(dashboard)/ops-room/page.tsx lines 277-289

export function OfficeViewSyb() {
  return null; // Rendered via PixelOffice in page.tsx
}
